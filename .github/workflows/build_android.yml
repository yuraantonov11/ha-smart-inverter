name: Build Android APK & AAB

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type (apk, aab, or both)'
        required: true
        default: 'both'
        type: choice
        options:
          - apk
          - aab
          - both

permissions:
  contents: write

jobs:
  build_android:
    name: Build Android
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version-file: pubspec.yaml

      - name: Install dependencies
        run: flutter pub get

      - name: Generate localization files
        run: flutter gen-l10n

      - name: Build Flutter
        run: flutter build apk --release --split-per-abi

      - name: Build Flutter AAB (if enabled)
        if: github.event.inputs.build_type == 'aab' || github.event.inputs.build_type == 'both' || github.ref_type == 'tag'
        run: flutter build appbundle --release

      - name: Prepare artifacts
        run: |
          mkdir -p artifacts
          if [ -d "build/app/outputs/flutter-apk" ]; then
            cp build/app/outputs/flutter-apk/app-*.apk artifacts/ 2>/dev/null || true
          fi
          if [ -f "build/app/outputs/bundle/release/app-release.aab" ]; then
            cp build/app/outputs/bundle/release/app-release.aab artifacts/
          fi

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v3
        if: success()
        with:
          name: android-apk-build-${{ github.run_id }}
          path: artifacts/app-*.apk
          retent

ion-days: 30

      - name: Upload AAB as artifact
        uses: actions/upload-artifact@v3
        if: success() && (github.event.inputs.build_type == 'aab' || github.event.inputs.build_type == 'both' || github.ref_type == 'tag')
        with:
          name: android-aab-build-${{ github.run_id }}
          path: artifacts/app-release.aab
          retention-days: 30

      - name: Create or Update Release (on tag)
        if: startsWith(github.ref, 'refs/tags/') && success()
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: artifacts/*
          draft: false
          prerelease: false
          generate_release_notes: true

  build_status:
    name: Build Status Notification
    runs-on: ubuntu-latest
    needs: build_android
    if: always()

    steps:
      - name: Check build status
        run: |
          if [ "${{ needs.build_android.result }}" == "success" ]; then
            echo "✅ Android build completed successfully!"
            exit 0
          else
            echo "❌ Android build failed!"
            exit 1
          fi

